# Get parameters from a yaml file

If GitHub Actions is running on the main branch, use prod. Otherwise
dev.

## Usage

``` r
get_params(path)
```

## Arguments

- path:

  String indicating path to the yaml file.
